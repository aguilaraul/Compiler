ALGORITHM:

* Read each line while striping comments, add trimmed part to overall string
    * Recursively remove comments
        * Strip remaining multi-line comments
        * Strip remaining Javadoc

- Begin tokenizing everything into an array list

- Take each token and decide which type, keyword, symbol, etc. it is
    - (whileHasMoreTokens)