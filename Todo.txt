TODO:

* Read each line then strip away single line comment, multi-line comments, and Javadoc comments

Tasks:
- After cleaning, split(" ") into different parts and store them in String[] or array list
- After storing every token, take each token and decide which type, keyword, symbol, etc. it is using whileHasMoreTokens